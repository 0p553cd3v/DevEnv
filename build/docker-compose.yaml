version: "3"
services:
  devenv: #NOTE:change container name
    image: devenv #NOTE:change image name
    networks:
    - docker-network
    restart: unless-stopped
    volumes:
    - $HOME/docker-volumes/devenv/dir:/home/dir    #NOTE:change mounted volumes
networks:
  docker-network:
#    driver: bridge
